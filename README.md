# QR Menu - Fullstack Application

Полнофункциональное приложение QR меню с React frontend и FastAPI backend.

## Особенности

- 🔐 Аутентификация пользователей (JWT)
- 👨‍💼 Админ-панель для управления продуктами и меню
- 🛒 Корзина и система заказов
- 📊 Статистика и аналитика
- 🤖 AI чат-бот с рекомендациями
- 🌐 Мультиязычность (Армянский, Английский, Русский)
- 📱 Адаптивный дизайн

## Структура проекта

```
QR_MENU/
├── qr_menu_backend/          # FastAPI Backend
│   ├── data/                 # JSON файлы для хранения данных
│   │   ├── users.json
│   │   ├── products.json
│   │   ├── menu.json
│   │   ├── orders.json
│   │   └── events.json
│   ├── routes/               # API endpoints
│   │   ├── auth.py          # Аутентификация
│   │   ├── admin.py         # Админ панель
│   │   ├── orders.py        # Заказы и корзина
│   │   └── products.py      # Продукты
│   ├── services/            # Сервисы (OpenAI)
│   ├── main.py              # Главный файл приложения
│   ├── models.py            # Pydantic модели
│   ├── utils.py             # Утилиты (JWT, bcrypt)
│   └── requirements.txt
│
└── qr_menu_front/reactmenu/  # React Frontend
    ├── src/
    │   ├── pages/           # Страницы
    │   │   ├── Login.js
    │   │   ├── Register.js
    │   │   └── admin/       # Админ панель
    │   ├── component/       # Компоненты
    │   ├── AuthContext.js   # Контекст авторизации
    │   └── App.js
    └── package.json
```

## Установка

### Backend

1. Перейдите в директорию backend:
```bash
cd qr_menu_backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` с вашим OpenAI API ключом:
```
OPENAI_API_KEY=your_api_key_here
```

5. Запустите сервер:
```bash
python main.py
```

Backend будет доступен на `http://localhost:8000`

### Frontend

1. Перейдите в директорию frontend:
```bash
cd qr_menu_front/reactmenu
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```
REACT_APP_API_URL=http://localhost:8000
```

4. Запустите приложение:
```bash
npm start
```

Frontend будет доступен на `http://localhost:3000`

## Учетные данные по умолчанию

**Админ:**
- Логин: `admin`
- Пароль: `123456`

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получить текущего пользователя

### Продукты
- `GET /api/products` - Получить продукты из меню (публичный)

### Корзина и Заказы
- `POST /api/cart/add` - Добавить в корзину (логирование события)
- `POST /api/orders/checkout` - Оформить заказ
- `GET /api/orders/my` - Мои заказы (требует авторизации)
- `GET /api/recommendations` - Получить рекомендации

### Админ (требует роль admin)
- `GET /api/admin/products` - Все продукты
- `POST /api/admin/products` - Создать продукт
- `PUT /api/admin/products/{id}` - Обновить продукт
- `DELETE /api/admin/products/{id}` - Удалить продукт
- `GET /api/admin/menu` - Получить меню
- `POST /api/admin/menu/{id}` - Добавить в меню
- `DELETE /api/admin/menu/{id}` - Удалить из меню
- `GET /api/admin/stats` - Статистика

## Роуты Frontend

### Для пользователей
- `/` - Главная страница (меню)
- `/login` - Вход для пользователей
- `/register` - Регистрация

### Для админов
- `/admin/login` - Вход в админ панель
- `/admin` - Админ панель (Dashboard)
- `/admin/products` - Управление продуктами
- `/admin/menu` - Управление меню
- `/admin/stats` - Статистика

## Хранение данных

Все данные хранятся в JSON файлах в директории `qr_menu_backend/data/`:

- **users.json** - Пользователи (пароли хешированы с bcrypt)
- **products.json** - Все продукты
- **menu.json** - ID продуктов, видимых в основном приложении
- **orders.json** - Заказы пользователей
- **events.json** - События (ADD_TO_CART, CHECKOUT)

## Технологии

### Backend
- FastAPI
- Pydantic
- bcrypt (хеширование паролей)
- PyJWT (JWT токены)
- OpenAI API (AI чат-бот)

### Frontend
- React 19
- React Router DOM
- Axios
- Context API (управление состоянием)
- CSS3 (адаптивный дизайн)

## Разработка

### Добавление нового продукта через админ панель

1. Войдите как админ
2. Перейдите в `/admin/products`
3. Нажмите "Ավելացնել ապրանք"
4. Заполните форму
5. Продукт создан, но не виден в меню

### Добавление продукта в меню

1. Перейдите в `/admin/menu`
2. Найдите продукт в разделе "Հասանելի ապրանքներ"
3. Нажмите "Ավելացնել մենյուին"
4. Продукт теперь виден в основном приложении

## Примечания

- Основной язык интерфейса - армянский
- Все данные хранятся локально в JSON файлах
- Нет базы данных - простая архитектура для MVP
- JWT токены хранятся в localStorage
- События пользователей логируются для статистики

## Лицензия

MIT
